+++
title = "Installation"
weight = 2
+++

```
curl -fsSL https://example.com/install.sh | sh   # Linux / macOS
irm https://example.com/install.ps1 | iex         # Windows (PowerShell)
```

> This page is a stub. Full documentation is not yet written.
>
> Planned content: installing via the `install.sh` / `install.ps1` scripts generated by cargo-dist; per-platform install locations; PATH setup; updating using the `ggg-update` binary that ships alongside `ggg`.
